<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="AffairList.Views.ProfilesPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Padding="24,24,24,0">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Profiles" FontSize="28" FontWeight="SemiBold" Margin="0,0,0,16"/>

        <ListView 
            Grid.Row="1"
            ItemsSource="{x:Bind ViewModel.Profiles, Mode=OneWay}"
            SelectedItem="{x:Bind ViewModel.SelectedProfile, Mode=TwoWay}"
            SelectionMode="Single">
            <ListView.ItemTemplate>
                <DataTemplate x:DataType="x:String">
                    <TextBlock Text="{x:Bind}" FontSize="16" Padding="0,12"/>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>

        <Grid Grid.Row="2" ColumnDefinitions="*, Auto, Auto" Margin="0,20,0,24" ColumnSpacing="12">
            <TextBox 
                Grid.Column="0" 
                PlaceholderText="New profile name..." 
                Text="{x:Bind ViewModel.NewProfileName, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                VerticalAlignment="Stretch"/>

            <Button 
                Grid.Column="1" 
                Content="Create" 
                Command="{x:Bind ViewModel.CreateProfileCommand}"
                Height="32"/>

            <Button 
                Grid.Column="2" 
                Content="Apply Selected" 
                Command="{x:Bind ViewModel.ChangeProfileCommand}"
                Style="{StaticResource AccentButtonStyle}"
                Height="32"/>
        </Grid>
    </Grid>
</Page>